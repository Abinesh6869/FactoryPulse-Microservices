package org.cts.fp_reporting.client;

// AnalyticsClient removed — fp_analytics has been merged into this service (fp_reporting_analytics).
// OEE data is now fetched directly via OEEService (in-process).
